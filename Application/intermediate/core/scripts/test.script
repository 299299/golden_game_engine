local test_func = function()
{
    while(true)
    {
        if(input.is_key_down(input.ESC))
        {
            shut_down();
        }
        else if(input.is_key_down(input.F1))
        {
            print(string_to_id("hello##"));
        }

        yield();
    }
};
test_func();